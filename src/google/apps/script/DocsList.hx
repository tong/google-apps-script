package google.apps.script;

@:require(google_apps_script)
@:native("DocsList")
extern class DocsList {
	var DEFAULT_RESULT_SIZE : Int;
	var FileType : FileType;
	var MAX_RESULT_SIZE : Int;
	//TODO,,
}